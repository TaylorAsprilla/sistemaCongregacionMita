import { RemoverEspaciosPipe } from './remover-espacios.pipe';

describe('RemoverEspaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoverEspaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
