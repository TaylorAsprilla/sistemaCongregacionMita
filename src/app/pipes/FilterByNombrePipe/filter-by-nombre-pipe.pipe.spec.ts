import { FilterByNombrePipePipe } from './filter-by-nombre-pipe.pipe';

describe('FilterByNombrePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNombrePipePipe();
    expect(pipe).toBeTruthy();
  });
});
