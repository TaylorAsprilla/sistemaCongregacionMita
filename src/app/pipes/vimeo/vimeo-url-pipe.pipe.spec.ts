import { VimeoUrlPipePipe } from './vimeo-url-pipe.pipe';

describe('VimeoUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VimeoUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
